This project now uses a top-level package `library_project` for settings and URLs.
Manage.py has been updated to use `library_project.settings`.
A new app `library` has been added for the main site functionality (home, about, contact).
Existing `books` app remains and is included under /books/.
